<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème minimal : l'app est 100% Compose, le thème réel (couleurs,
         typographie) est défini dans le code Kotlin (ui/theme). Ce thème XML
         ne sert qu'à fixer la fenêtre (pas de barre d'action, fond sombre
         cohérent avec l'app pendant le tout premier instant de lancement). -->
    <style name="Theme.MonNhuitNe" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorBackground</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
    </style>
</resources>
